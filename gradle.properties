# Reuse compilation and other cacheable task outputs across worktrees and CI.
# Test-result caching is disabled in build.gradle.kts until external inputs are declared.
# Explicit --no-build-cache (including cold research verification) still overrides this.
org.gradle.caching=true
